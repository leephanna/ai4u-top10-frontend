import React, { useState } from 'react';
import { Loader2, Star, ExternalLink, Copy, CheckCircle, Mail, Share2, Facebook, Twitter, Linkedin, MessageCircle } from 'lucide-react';
import './App.css';

const API_BASE_URL = 'https://ai4u-top10-backend.vercel.app';

const AmazonTop10App = () => {
  const [prompt, setPrompt] = useState('');
  const [email, setEmail] = useState('');
  const [loading, setLoading] = useState(false);
  const [result, setResult] = useState(null);
  const [copied, setCopied] = useState(false);
  const [emailSent, setEmailSent] = useState(false);

  const generateList = async () => {
    if (!prompt.trim()) return;
    
    setLoading(true);
    setResult(null);
    setEmailSent(false);
    
    try {
      const response = await fetch(`${API_BASE_URL}/api/generate-list`, {
        method: 'POST',
        headers: {
          'Content-Type': 'application/json',
        },
        body: JSON.stringify({ 
          prompt: prompt.trim(),
          email: email.trim() 
        }),
      });
      
      const data = await response.json();
      
      if (data.success) {
        setResult(data);
        if (email.trim()) {
          setEmailSent(true);
        }
      } else {
        setResult({
          success: false,
          error: data.error || 'Failed to generate list'
        });
      }
    } catch (error) {
      setResult({
        success: false,
        error: 'Network error - please try again'
      });
    } finally {
      setLoading(false);
    }
  };

  const shareToSocial = async (platform) => {
    if (!result?.share_url) return;
    
    const shareUrls = {
      facebook: `https://www.facebook.com/sharer/sharer.php?u=${encodeURIComponent(result.share_url)}`,
      twitter: `https://twitter.com/intent/tweet?url=${encodeURIComponent(result.share_url)}&text=${encodeURIComponent('Check out this AI-researched Top 10 list!')}`,
      linkedin: `https://www.linkedin.com/sharing/share-offsite/?url=${encodeURIComponent(result.share_url)}`,
      whatsapp: `https://wa.me/?text=${encodeURIComponent(`Check out this Top 10 list: ${result.share_url}`)}`,
      email: `mailto:?subject=${encodeURIComponent('Top 10 List')}&body=${encodeURIComponent(`Check out this list: ${result.share_url}`)}`
    };
    
    window.open(shareUrls[platform], '_blank', 'width=600,height=400');
  };

  const copyToClipboard = () => {
    if (!result || !result.success) return;
    
    const htmlContent = generateHTMLOutput(result);
    navigator.clipboard.writeText(htmlContent).then(() => {
      setCopied(true);
      setTimeout(() => setCopied(false), 2000);
    });
  };

  const generateHTMLOutput = (data) => {
    const productsHTML = data.products.map((product, index) => `
      <div class="product-item" style="border: 1px solid #ddd; margin: 10px 0; padding: 15px; border-radius: 8px;">
        <h3 style="color: #232F3E;">${index + 1}. ${product.title}</h3>
        <div style="display: flex; align-items: center; margin: 5px 0;">
          <span style="color: #FF9900; font-weight: bold;">${product.price}</span>
          <span style="margin-left: 15px; color: #565959;">⭐ ${product.rating}/5</span>
        </div>
        <p style="color: #565959; margin: 10px 0;">${product.description}</p>
        <a href="${product.affiliate_link}" target="_blank" 
           style="background: #FF9900; color: white; padding: 10px 20px; text-decoration: none; border-radius: 4px; display: inline-block;">
          View on Amazon
        </a>
      </div>
    `).join('');

    return `
      <div style="max-width: 800px; margin: 0 auto; font-family: Arial, sans-serif;">
        <h1 style="color: #232F3E; text-align: center;">${data.title}</h1>
        <p style="color: #565959; text-align: center; margin: 20px 0;">${data.intro}</p>
        ${productsHTML}
        <p style="text-align: center; color: #565959; font-size: 12px; margin-top: 30px;">
          Generated by AI4U Top 10 Lists • ${data.generated_at}
        </p>
      </div>
    `;
  };

  return (
    <div className="min-h-screen bg-gradient-to-br from-indigo-900 via-purple-900 to-pink-800">
      <div className="container mx-auto px-4 py-8">
        <div className="text-center mb-8">
          <h1 className="text-4xl font-bold text-white mb-4">
            AI4U Top 10 Lists ✨
          </h1>
          <p className="text-lg text-indigo-200 max-w-2xl mx-auto">
            AI-Researched & Endorsed Amazon Products - Now with REAL data and working affiliate links!
          </p>
        </div>

        <div className="max-w-2xl mx-auto mb-8">
          <div className="bg-white/10 backdrop-blur-lg rounded-2xl p-6 border border-white/20">
            <div className="mb-4">
              <label className="block text-white text-sm font-medium mb-2">
                What products are you looking for?
              </label>
              <input
                type="text"
                value={prompt}
                onChange={(e) => setPrompt(e.target.value)}
                placeholder="e.g., organic potato chips, baby diapers, gaming laptops..."
                className="w-full px-4 py-3 rounded-lg bg-white/20 border border-white/30 text-white placeholder-white/60 focus:outline-none focus:ring-2 focus:ring-purple-400"
                onKeyPress={(e) => e.key === 'Enter' && !loading && generateList()}
              />
            </div>

            <div className="mb-4">
              <label className="block text-white text-sm font-medium mb-2">
                Email address (optional - to receive your list)
              </label>
              <input
                type="email"
                value={email}
                onChange={(e) => setEmail(e.target.value)}
                placeholder="your@email.com"
                className="w-full px-4 py-3 rounded-lg bg-white/20 border border-white/30 text-white placeholder-white/60 focus:outline-none focus:ring-2 focus:ring-purple-400"
              />
            </div>
            
            <button
              onClick={generateList}
              disabled={loading || !prompt.trim()}
              className="w-full bg-gradient-to-r from-purple-600 to-pink-600 text-white py-3 px-6 rounded-lg font-semibold hover:from-purple-700 hover:to-pink-700 disabled:opacity-50 disabled:cursor-not-allowed flex items-center justify-center gap-2"
            >
              {loading ? (
                <>
                  <Loader2 className="animate-spin" size={20} />
                  Researching Products...
                </>
              ) : (
                'Generate Top 10 List'
              )}
            </button>
          </div>
        </div>

        {result && (
          <div className="max-w-4xl mx-auto">
            {result.success ? (
              <div className="bg-white/10 backdrop-blur-lg rounded-2xl p-6 border border-white/20">
                <div className="flex justify-between items-start mb-6">
                  <div className="flex-grow">
                    <h2 className="text-2xl font-bold text-white mb-2">
                      {result.title}
                    </h2>
                    <p className="text-indigo-200">
                      {result.intro}
                    </p>
                    <p className="text-sm text-green-300 mt-2">
                      ✅ All products have REAL Amazon ASINs and working affiliate links!
                    </p>
                    {emailSent && (
                      <p className="text-sm text-blue-300 mt-1">
                        📧 List sent to your email!
                      </p>
                    )}
                  </div>
                  <div className="flex gap-2 ml-4">
                    <button
                      onClick={copyToClipboard}
                      className="bg-green-600 hover:bg-green-700 text-white px-4 py-2 rounded-lg flex items-center gap-2"
                    >
                      {copied ? <CheckCircle size={16} /> : <Copy size={16} />}
                      {copied ? 'Copied!' : 'Copy HTML'}
                    </button>
                  </div>
                </div>

                <div className="mb-6 p-4 bg-white/10 rounded-lg">
                  <h3 className="text-white font-semibold mb-3 flex items-center gap-2">
                    <Share2 size={18} />
                    Share this list
                  </h3>
                  <div className="flex flex-wrap gap-2">
                    <button
                      onClick={() => shareToSocial('facebook')}
                      className="bg-blue-600 hover:bg-blue-700 text-white px-3 py-2 rounded-lg flex items-center gap-2 text-sm"
                    >
                      <Facebook size={16} />
                      Facebook
                    </button>
                    <button
                      onClick={() => shareToSocial('twitter')}
                      className="bg-sky-500 hover:bg-sky-600 text-white px-3 py-2 rounded-lg flex items-center gap-2 text-sm"
                    >
                      <Twitter size={16} />
                      Twitter
                    </button>
                    <button
                      onClick={() => shareToSocial('linkedin')}
                      className="bg-blue-700 hover:bg-blue-800 text-white px-3 py-2 rounded-lg flex items-center gap-2 text-sm"
                    >
                      <Linkedin size={16} />
                      LinkedIn
                    </button>
                    <button
                      onClick={() => shareToSocial('whatsapp')}
                      className="bg-green-500 hover:bg-green-600 text-white px-3 py-2 rounded-lg flex items-center gap-2 text-sm"
                    >
                      <MessageCircle size={16} />
                      WhatsApp
                    </button>
                    <button
                      onClick={() => shareToSocial('email')}
                      className="bg-gray-600 hover:bg-gray-700 text-white px-3 py-2 rounded-lg flex items-center gap-2 text-sm"
                    >
                      <Mail size={16} />
                      Email
                    </button>
                  </div>
                  <div className="mt-2">
                    <input
                      type="text"
                      value={result.share_url || 'https://ai4u-top10-lists.vercel.app'}
                      readOnly
                      className="w-full px-3 py-2 bg-white/20 border border-white/30 text-white text-sm rounded"
                      onClick={(e) => e.target.select()}
                    />
                  </div>
                </div>

                <div className="grid gap-4">
                  {result.products.map((product, index) => (
                    <div key={product.asin} className="bg-white/20 rounded-lg p-4 border border-white/30">
                      <div className="flex items-start gap-4">
                        <div className="flex-shrink-0">
                          <div className="w-12 h-12 bg-gradient-to-br from-purple-500 to-pink-500 rounded-full flex items-center justify-center text-white font-bold">
                            {index + 1}
                          </div>
                        </div>
                        
                        <div className="flex-grow">
                          <h3 className="text-lg font-semibold text-white mb-2">
                            {product.title}
                          </h3>
                          
                          <div className="flex items-center gap-4 mb-2">
                            <span className="text-green-400 font-bold text-xl">
                              {product.price}
                            </span>
                            <div className="flex items-center gap-1">
                              <Star className="text-yellow-400 fill-current" size={16} />
                              <span className="text-white">{product.rating}/5</span>
                            </div>
                          </div>
                          
                          <p className="text-indigo-200 text-sm mb-3">
                            {product.description}
                          </p>
                          
                          <div className="flex gap-2">
                            
                              href={product.affiliate_link}
                              target="_blank"
                              rel="noopener noreferrer"
                              className="bg-orange-500 hover:bg-orange-600 text-white px-4 py-2 rounded-lg text-sm flex items-center gap-2"
                            >
                              <ExternalLink size={14} />
                              View on Amazon
                            </a>
                            <span className="text-xs text-green-300 px-2 py-2">
                              ASIN: {product.asin}
                            </span>
                          </div>
                        </div>
                      </div>
                    </div>
                  ))}
                </div>

                <div className="mt-6 pt-4 border-t border-white/20 text-center">
                  <p className="text-sm text-indigo-200">
                    Generated: {result.generated_at} | Affiliate ID: {result.affiliate_id}
                  </p>
                  <p className="text-xs text-indigo-300 mt-1">
                    All links are functional and revenue-ready ✅
                  </p>
                </div>
              </div>
            ) : (
              <div className="bg-red-500/20 border border-red-500/50 rounded-lg p-4">
                <p className="text-red-200">
                  Error: {result.error}
                </p>
              </div>
            )}
          </div>
        )}

        <div className="max-w-4xl mx-auto mt-8 grid md:grid-cols-3 gap-4">
          <div className="bg-white/10 backdrop-blur-lg rounded-lg p-4 border border-white/20 text-center">
            <h3 className="text-white font-semibold mb-2">🎯 Unlimited Categories</h3>
            <p className="text-indigo-200 text-sm">From potato chips to baby products - any category works!</p>
          </div>
          <div className="bg-white/10 backdrop-blur-lg rounded-lg p-4 border border-white/20 text-center">
            <h3 className="text-white font-semibold mb-2">✅ Real Amazon Data</h3>
            <p className="text-indigo-200 text-sm">Actual ASINs, current prices, real customer ratings</p>
          </div>
          <div className="bg-white/10 backdrop-blur-lg rounded-lg p-4 border border-white/20 text-center">
            <h3 className="text-white font-semibold mb-2">💰 Revenue Ready</h3>
            <p className="text-indigo-200 text-sm">Working affiliate links with your ID: ai4u0c-20</p>
          </div>
        </div>
      </div>
    </div>
  );
};

export default AmazonTop10App;
